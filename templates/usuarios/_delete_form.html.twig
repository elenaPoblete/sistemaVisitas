<form method="post" action="{{ path('usuarios_delete', {'id': usuario.id}) }}" onsubmit="return confirm('¿Estás segura de que quieres eliminar este artículo?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ usuario.id) }}">
   <br>

    <button class="btn btn-danger" >Eliminar</button>
</form>
