{% extends 'base.html.twig' %}

{% block title %}Usuarios{% endblock %}

{% block body %}
    <h1>Usuarios</h1>

    <table class="table">
        <tbody>
            <tr>
                <th >Id </th>
                <td>{{ usuario.id }}</td>
            </tr>
            <tr>
                <th>Rut</th>
                <td>{{ usuario.rut }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ usuario.nombre }}</td>
            </tr>
            <tr>
                <th>Apellidos</th>
                <td>{{ usuario.apellidos }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ usuario.email }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('usuarios_index') }}">Volver a la Lista</a>

    <a class="btn btn-success" href="{{ path('usuarios_edit', {'id': usuario.id}) }}">Editar</a>

    {{ include('usuarios/_delete_form.html.twig') }}
{% endblock %}
